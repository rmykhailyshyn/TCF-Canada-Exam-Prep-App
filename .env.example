# PostgreSQL connection string used by Drizzle (server + migrations).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tcf_prep

# Express server port (client dev server proxies /api to this).
PORT=3001
