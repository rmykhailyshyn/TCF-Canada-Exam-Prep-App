node_modules/
dist/
build/
*.local

# Environment
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*

# Python virtualenv for the import pipeline (scripts/)
.venv/
.venv-inspect/
scripts/.venv/
__pycache__/

# Playwright e2e artifacts (the suite is committed; its outputs are not)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Coverage reports (unit + e2e client/server + combined) and nyc merge temp — all generated (M18)
coverage/
.nyc_output
.nyc_output/

# Local media directory (MEDIA_DIR): imported audio + dev-seed placeholder images, all generated
/media/

# Local SQLite database files (DATABASE_URL defaults to file:./data/tcf_prep.db; e2e uses its own)
/data/
*.db
*.db-shm
*.db-wal

# Generated listening dev clips (e2e global-setup regenerates them with ffmpeg)
scripts/dev-audio/

# Claude Code config is tracked (agents, commands, hooks, shared settings);
# only the machine-specific local permission grants stay out.
.claude/settings.local.json

# Graphify knowledge-graph output (generated tool artifacts)
graphify-out/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.tsbuildinfo
